services:
  web:
    build: .
    image: carloscapote-com:latest
    container_name: carloscapote-com
    restart: unless-stopped
    ports:
      - "8080:80"
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
